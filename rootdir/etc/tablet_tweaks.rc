# Redefine the vendor vibrator service as disabled since this tablet does not have a vibrator motor
service vendor.vibrator-default /vendor/bin/hw/android.hardware.vibrator-service.mediatek
    class hal
    user system
    group system
    disabled
